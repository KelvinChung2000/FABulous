# Generated by FABulous SDC loop-break export
# Scope: {{ scope }}
# Breaks combinational loops so STA (OpenSTA / Tempus / PrimeTime)
# can level-order the timing graph and proceed.
{% if not resolved %}
# WARNING: net names below are logical switch-matrix names and may not match a synthesized netlist.
{% endif %}

{% if break_nodes %}
{% for net in break_nodes %}
# break loop(s) through net {{ net }}
set_disable_timing [get_pins -of_objects [get_nets {{ net }}] -filter "direction==output"]
{% endfor %}
{% else %}
# no combinational loops found
{% endif %}
